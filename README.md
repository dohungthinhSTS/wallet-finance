# Wallet Finance

* docker setup:
  * `docker-compose up -d`

```bash
# setup example
$ git clone git@github.com:dohungthinhSTS/wallet-finance.git
$ cd wallet-finance
$ docker-compose up -d
```

# Go to browser: localhost:3006